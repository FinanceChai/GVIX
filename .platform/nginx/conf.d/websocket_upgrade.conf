# http-level map for WebSocket connection upgrades.
# Included by Elastic Beanstalk's nginx inside the http {} block.
map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
